[pytest]
# The workspace can be mounted without permission to create pytest's temporary
# cache helper directory. Disabling the optional cache keeps CI/test output
# clean and does not affect test correctness.
addopts = -p no:cacheprovider
